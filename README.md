# VeriSync

## GitHub OAuth callback

Set the GitHub App authorization callback URL to:

- `https://<your-render-backend>/auth/callback`

If `GITHUB_REDIRECT_URI` is set, it must match that exact URL.